<Page x:Class="Uno.Toolkit.Samples.Content.Controls.GridExtensionsSamplePage"
	  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	  xmlns:sample="using:Uno.Toolkit.Samples"
	  xmlns:utu="using:Uno.Toolkit.UI"
	  mc:Ignorable="d"
	  Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

	<Grid>
		<sample:SamplePageLayout x:Name="SamplePageLayout" IsDesignAgnostic="True">
			<sample:SamplePageLayout.DesignAgnosticTemplate>
				<DataTemplate>
					<ScrollViewer HorizontalScrollMode="Disabled">
						<StackPanel>
							<sample:ControlExample>
								
								<sample:ControlExample.Content>
									<Border BorderBrush="{ThemeResource OutlineVariantBrush}"
											BorderThickness="1"
											MinHeight="200">
										<Grid x:Name="DemoGrid" />
									</Border>
								</sample:ControlExample.Content>

								<sample:ControlExample.Options>
									<StackPanel Spacing="8">
										<CheckBox x:Name="OptionAuto"
												  Content="Auto"
												  IsChecked="True" />
										<TextBox x:Name="OptionColumns"
												 Header="Columns"
												 PlaceholderText="e.g. *,*,*" />
										<TextBox x:Name="OptionRows"
												 Header="Rows"
												 PlaceholderText="e.g. Auto,Auto" />
										<StackPanel Orientation="Horizontal" Spacing="8">
											<Button x:Name="OptionAdd" Content="Add" />
											<Button x:Name="OptionRemove" Content="Remove" />
											<Button x:Name="OptionReset" Content="Reset" />
										</StackPanel>
									</StackPanel>
								</sample:ControlExample.Options>
								
							</sample:ControlExample>
						</StackPanel>
					</ScrollViewer>
				</DataTemplate>
			</sample:SamplePageLayout.DesignAgnosticTemplate>
		</sample:SamplePageLayout>
	</Grid>
</Page>
