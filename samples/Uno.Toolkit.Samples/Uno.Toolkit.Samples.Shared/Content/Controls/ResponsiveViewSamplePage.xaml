<Page x:Class="Uno.Toolkit.Samples.Content.Controls.ResponsiveViewSamplePage"
	  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	  xmlns:local="using:Uno.Toolkit.Samples.Content.Controls"
	  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	  xmlns:utu="using:Uno.Toolkit.UI"
	  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	  xmlns:sample="using:Uno.Toolkit.Samples"
	  mc:Ignorable="d"
	  Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

	<Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
		<sample:SamplePageLayout x:Name="SamplePageLayout" IsDesignAgnostic="True">
			<sample:SamplePageLayout.DesignAgnosticTemplate>
				<DataTemplate>
					<StackPanel>
						<StackPanel.Resources>
							<x:Double x:Key="ResponsiveViewExtraNarrow">150</x:Double>
							<x:Double x:Key="ResponsiveViewNarrow">350</x:Double>
							<x:Double x:Key="ResponsiveViewDefault">800</x:Double>
							<x:Double x:Key="ResponsiveViewWide">1200</x:Double>
							<x:Double x:Key="ResponsiveViewExtraWide">1500</x:Double>

							<Style TargetType="utu:ResponsiveView"
								   x:Key="ResponsiveViewStyle">
								<Style.Setters>
									<Setter Property="Template">
										<Setter.Value>
											<ControlTemplate TargetType="utu:ResponsiveView">
												<Grid x:Name="RootElement">
													<VisualStateManager.VisualStateGroups>
														<VisualStateGroup x:Name="CommonStates">
															<VisualState x:Name="ExtraNarrowSize">
																<VisualState.StateTriggers>
																	<AdaptiveTrigger MinWindowWidth="{StaticResource ResponsiveViewExtraNarrow}" />
																</VisualState.StateTriggers>
															</VisualState>
															<VisualState x:Name="NarrowSize">
																<VisualState.StateTriggers>
																	<AdaptiveTrigger MinWindowWidth="{StaticResource ResponsiveViewNarrow}" />
																</VisualState.StateTriggers>
															</VisualState>
															<VisualState x:Name="DefaultSize">
																<VisualState.StateTriggers>
																	<AdaptiveTrigger MinWindowWidth="{StaticResource ResponsiveViewDefault}" />
																</VisualState.StateTriggers>
															</VisualState>
															<VisualState x:Name="WideSize">
																<VisualState.StateTriggers>
																	<AdaptiveTrigger MinWindowWidth="{StaticResource ResponsiveViewWide}" />
																</VisualState.StateTriggers>
															</VisualState>
															<VisualState x:Name="ExtraWideSize">
																<VisualState.StateTriggers>
																	<AdaptiveTrigger MinWindowWidth="{StaticResource ResponsiveViewExtraWide}" />
																</VisualState.StateTriggers>
															</VisualState>
														</VisualStateGroup>
													</VisualStateManager.VisualStateGroups>
													<ContentPresenter Content="{TemplateBinding Content}" />
												</Grid>
											</ControlTemplate>
										</Setter.Value>
									</Setter>
								</Style.Setters>
							</Style>
						</StackPanel.Resources>
						<utu:ResponsiveView Style="{StaticResource ResponsiveViewStyle}">
							<utu:ResponsiveView.ExtraNarrowContent>
								<DataTemplate>
									<TextBlock Text="Extra Narrow" 
											   Style="{StaticResource DisplayLarge}" />
								</DataTemplate>
							</utu:ResponsiveView.ExtraNarrowContent>
							<utu:ResponsiveView.NarrowContent>
								<DataTemplate>
									<TextBlock Text="Narrow" 
											   Style="{StaticResource DisplayLarge}" />
								</DataTemplate>
							</utu:ResponsiveView.NarrowContent>
							<utu:ResponsiveView.DefaultContent>
								<DataTemplate>
									<TextBlock Text="Default" 
											   Style="{StaticResource DisplayLarge}" />
								</DataTemplate>
							</utu:ResponsiveView.DefaultContent>
							<utu:ResponsiveView.WideContent>
								<DataTemplate>
									<TextBlock Text="Wide"
											   Style="{StaticResource DisplayLarge}" />
								</DataTemplate>
							</utu:ResponsiveView.WideContent>
							<utu:ResponsiveView.ExtraWideContent>
								<DataTemplate>
									<TextBlock Text="Extra Wide"
											   Style="{StaticResource DisplayLarge}" />
								</DataTemplate>
							</utu:ResponsiveView.ExtraWideContent>
						</utu:ResponsiveView>
					</StackPanel>
				</DataTemplate>
			</sample:SamplePageLayout.DesignAgnosticTemplate>
		</sample:SamplePageLayout>
	</Grid>
</Page>
