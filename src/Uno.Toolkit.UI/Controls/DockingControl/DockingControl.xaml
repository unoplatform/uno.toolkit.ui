<UserControl x:Class="Uno.Toolkit.UI.DockingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:local="using:Uno.Toolkit.UI"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

  <Grid x:Name="MainGrid">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <!-- will not be part of the control, move to sample -->
    <Button Content="Add New Panel"
            Grid.Row="0"
            Grid.Column="0"
            Click="OnAddPanelClicked"
            Margin="10" />

		<local:DockPanel x:Name="DCHolder"
						 LastChildFill="True"
						 Grid.Row="1" />

	</Grid>
</UserControl>
