<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
					xmlns:utu="using:Uno.Toolkit.UI"
					xmlns:win="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:android="http://uno.ui/android"
					xmlns:ios="http://uno.ui/ios"
					xmlns:wasm="http://uno.ui/wasm"
					xmlns:macos="http://uno.ui/macos"
					xmlns:toolkit="using:Uno.UI.Toolkit"
					mc:Ignorable="android ios wasm macos">

	<ItemsPanelTemplate x:Key="MaterialHorizontalChipGroupItemsPanel">
		<StackPanel Orientation="Horizontal"
					Spacing="8" />
	</ItemsPanelTemplate>

	<Style x:Key="M3MaterialBaseChipGroupStyle"
		   TargetType="utu:ChipGroup">
		<Setter Property="ItemsPanel"
				Value="{StaticResource MaterialHorizontalChipGroupItemsPanel}" />
	</Style>

	<!--#region Input Chip Group Style -->
	<Style x:Key="M3MaterialInputChipGroupStyle"
		   BasedOn="{StaticResource M3MaterialBaseChipGroupStyle}"
		   TargetType="utu:ChipGroup">
		<Setter Property="ItemContainerStyle"
				Value="{StaticResource M3MaterialInputChipStyle}" />
		<Setter Property="CanRemove"
				Value="True" />
		<Setter Property="SelectionMode"
				Value="Multiple" />
	</Style>
	<!--#endregion -->

	<!--#region Suggestion Chip Group Style -->
	<Style x:Key="M3MaterialElevatedSuggestionChipGroupStyle"
		   BasedOn="{StaticResource M3MaterialBaseChipGroupStyle}"
		   TargetType="utu:ChipGroup">
		<Setter Property="ItemContainerStyle"
				Value="{StaticResource M3MaterialElevatedSuggestionChipStyle}" />
		<Setter Property="CanRemove"
				Value="False" />
		<Setter Property="SelectionMode"
				Value="Single" />
	</Style>

	<Style x:Key="M3MaterialSuggestionChipGroupStyle"
		   BasedOn="{StaticResource M3MaterialBaseChipGroupStyle}"
		   TargetType="utu:ChipGroup">
		<Setter Property="ItemContainerStyle"
				Value="{StaticResource M3MaterialSuggestionChipStyle}" />
		<Setter Property="CanRemove"
				Value="False" />
		<Setter Property="SelectionMode"
				Value="Single" />
	</Style>
	<!--#endregion-->

	<!--#region Filter Chip Group Style -->
	<Style x:Key="M3MaterialElevatedFilterChipGroupStyle"
		   BasedOn="{StaticResource M3MaterialBaseChipGroupStyle}"
		   TargetType="utu:ChipGroup">
		<Setter Property="ItemContainerStyle"
				Value="{StaticResource M3MaterialElevatedFilterChipStyle}" />
		<Setter Property="CanRemove"
				Value="False" />
		<Setter Property="SelectionMode"
				Value="Multiple" />
	</Style>

	<Style x:Key="M3MaterialFilterChipGroupStyle"
		   BasedOn="{StaticResource M3MaterialBaseChipGroupStyle}"
		   TargetType="utu:ChipGroup">
		<Setter Property="ItemContainerStyle"
				Value="{StaticResource M3MaterialFilterChipStyle}" />
		<Setter Property="CanRemove"
				Value="False" />
		<Setter Property="SelectionMode"
				Value="Multiple" />
	</Style>
	<!--#endregion-->

	<!-- #region Assist Chip Group Style -->
	<Style x:Key="M3MaterialElevatedAssistChipGroupStyle"
		   BasedOn="{StaticResource M3MaterialBaseChipGroupStyle}"
		   TargetType="utu:ChipGroup">
		<Setter Property="ItemContainerStyle"
				Value="{StaticResource M3MaterialElevatedAssistChipStyle}" />
		<Setter Property="CanRemove"
				Value="False" />
		<Setter Property="SelectionMode"
				Value="None" />
	</Style>

	<Style x:Key="M3MaterialAssistChipGroupStyle"
		   BasedOn="{StaticResource M3MaterialBaseChipGroupStyle}"
		   TargetType="utu:ChipGroup">
		<Setter Property="ItemContainerStyle"
				Value="{StaticResource M3MaterialAssistChipStyle}" />
		<Setter Property="CanRemove"
				Value="False" />
		<Setter Property="SelectionMode"
				Value="None" />
	</Style>
	<!--#endregion-->

	<!--
		ItemsWrapGrid is not working inside an ItemsControl.
		Issue (WASM) https://github.com/unoplatform/uno/issues/468
		(All platforms) https://github.com/unoplatform/uno/issues/4023
	-->
	<!--<ItemsPanelTemplate x:Key="MaterialWrapChipGroupItemsPanel">
		<ItemsWrapGrid Orientation="Horizontal" />
	</ItemsPanelTemplate>

	
	<Style x:Key="MaterialWrapChipGroupStyle"
		   TargetType="utu:ChipGroup">
		<Setter Property="ItemsPanel"
				Value="{StaticResource MaterialWrapChipGroupItemsPanel}" />
		<Setter Property="ItemContainerStyle"
				Value="{StaticResource MaterialChipStyle}" />
	</Style>-->

</ResourceDictionary>
