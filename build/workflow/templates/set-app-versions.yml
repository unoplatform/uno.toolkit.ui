steps:
  - pwsh: dotnet build $(build.sourcesdirectory)/build/workflow/build.csproj /r /t:BuildVersion /p:AppName=Uno.Toolkit.Samples /bl:$(build.artifactstagingdirectory)//set-version-winui-build.binlog
    displayName: Update app versions