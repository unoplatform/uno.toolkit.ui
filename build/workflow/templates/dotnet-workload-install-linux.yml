parameters:
  # DotNetVersion must not exceed the SDK uno.check's manifest provisions wasm-tools for:
  # installing it sets the mono.toolchain.current manifest, and if that lands BELOW the running
  # SDK the resolver asks for a Microsoft.NET.Runtime.MonoTargets.Sdk that was never installed,
  # failing every net10.0-ios library build with NETSDK1147. uno.check 1.34.1 now resolves
  # wasm-tools 10.0.108, so 10.0.102 is covered; re-check when bumping either version.
  DotNetVersion: '10.0.102'
  UnoCheck_Version: '1.34.1'
  Dotnet_Tools: '~/.dotnet/tools'
  UnoCheckParameters: ''

steps:
  - template: dotnet-install.yml
    parameters:
      UnoCheckParameters: '${{ parameters.UnoCheckParameters }}'
      UnoCheck_Version: '${{ parameters.UnoCheck_Version }}'
      DotNetVersion: '${{ parameters.DotNetVersion }}'

  - template: jdk-setup.yml

  - bash: |
      dotnet tool update --global uno.check --version ${{ parameters.UnoCheck_Version }} --add-source https://api.nuget.org/v3/index.json
      uno-check --verbose --ci --non-interactive --fix --skip gtk3 --skip androidsdk --pre-major ${{ parameters.UnoCheckParameters }}
    displayName: Install .NET Workloads
    condition: and(succeeded(), eq(variables['Agent.OS'], 'Linux'))
