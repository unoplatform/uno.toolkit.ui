name: "Copilot Setup Steps"

# Automatically run the setup steps when they are changed to allow for easy validation, and
# allow manual testing through the repository's "Actions" tab
on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/copilot-setup-steps.yml
  pull_request:
    paths:
      - .github/workflows/copilot-setup-steps.yml

jobs:
  # The job MUST be called `copilot-setup-steps` or it will not be picked up by Copilot.
  copilot-setup-steps:
    runs-on: ubuntu-latest
    environment: copilot

    # Set the permissions to the lowest permissions possible needed for your steps.
    # Copilot will be given its own token for its operations.
    permissions:
      # If you want to clone the repository as part of your setup steps, for example to install dependencies, you'll need the `contents: read` permission. If you don't clone the repository in your setup steps, Copilot will do this for you automatically after the steps complete.
      contents: read

    # IMPORTANT: The steps here must use separate script files under `build/ci/scripts/` to ensure
    # that they can be altered by PRs (this file is only read from the default branch).
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Run uno-check (desktop + wasm)
        run: bash build/workflow/copilot/scripts/run-uno-check.sh

      - name: Start virtual display (xvfb)
        run: bash build/workflow/copilot/scripts/setup-virtual-display.sh

      - name: Restore solution packages
        run: bash build/workflow/copilot/scripts/restore-solution.sh